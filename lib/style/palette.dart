import 'package:flutter/material.dart';

abstract class Palette{
  static const primaryColor = Color(0xFF00B050);
  static const secondaryColor = Colors.white;
  static const buttonColor = Color(0xFF006666);
}